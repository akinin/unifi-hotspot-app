#include <tunables/global>

profile wirenboard_sms flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  network,
  signal (send) set=(kill,term,int,hup,cont),

  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/lib/bashio/** ix,
  /run/{s6,s6-rc*,service}/** rwix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,
  /dev/ttyUSB* rw,
  /dev/ttyACM* rw,
  /dev/cdc-wdm* rw,
  /tmp/** rwk,
  /data/** rwk,
  /opt/sms-gateway/** r,
}
