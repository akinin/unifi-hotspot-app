#!/usr/bin/with-contenv bashio
if [[ "$(bashio::config 'sms_backend')" != "mmcli" ]]; then
    exec sleep infinity
fi
mkdir -p /run/dbus
exec dbus-daemon --session --address=unix:path=/run/dbus/system_bus_socket --nofork --nopidfile
